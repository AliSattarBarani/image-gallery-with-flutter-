import 'package:flutter/material.dart';

class PLScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Programming Layout',style: TextStyle(fontSize: 40.0),),
      ),
    );
  }
}