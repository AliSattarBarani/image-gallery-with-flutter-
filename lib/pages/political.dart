import 'package:flutter/material.dart';

class PoliticalScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Political Layout',style: TextStyle(fontSize: 40.0),),
      ),
    );
  }
}